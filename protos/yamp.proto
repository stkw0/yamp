syntax = "proto3";

package yamp;

service Server {
  // Sends a greeting
  rpc Play(Null) returns (Null);
  rpc Pause(Null) returns (Null);
  rpc GetArtist(Null) returns (Artist);
}

message Null {
}

message Artist {
	string artist = 1;
}


